apiVersion: v1
kind: Service
metadata:
  name: seaweedfs
  namespace: open-cluster-management-observability
spec:
  ports:
  - port: 8333
    protocol: TCP
    targetPort: 8333
  selector:
    app.kubernetes.io/name: seaweedfs
  type: ClusterIP
