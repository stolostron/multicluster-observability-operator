apiVersion: apps/v1
kind: Deployment
metadata:
  name: endpoint-monitoring-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: endpoint-monitoring-operator
  template:
    metadata:
      labels:
        name: endpoint-monitoring-operator
    spec:
      serviceAccountName: endpoint-monitoring-operator-sa
      containers:
        - name: endpoint-monitoring-operator
          # Replace this with the built image name
          image: "quay.io/open-cluster-management/endpoint-monitoring-operator:0.1.0-9ccd9631ce8f0abe2b327d39b15752ec4c48a1fe"
          command:
          - endpoint-monitoring-operator
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              value: REPLACE_WITH_HUB_CLUSTER_NAMESPACE
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "endpoint-monitoring-operator"
            - name: HUB_KUBECONFIG
              value: "/spoke/hub-kubeconfig/config"
          volumeMounts:
            - name: hub-kubeconfig-secret
              mountPath: "/spoke/hub-kubeconfig"
              readOnly: true
      volumes:
        - name: hub-kubeconfig-secret
          secret:
            secretName: hub-kube-config
