apiVersion: apps/v1
kind: Deployment
metadata:
  name: observatorium-operator
  labels:
    app.kubernetes.io/component: observatorium-operator
    app.kubernetes.io/name: observatorium-operator
    app.kubernetes.io/part-of: observatorium
    control-plane: observatorium-operator
    monitoring.open-cluster-management.io/name: "{{MULTICLUSTERMONITORING_CR_NAME}}"
spec:
  selector:
    matchLabels:
      control-plane: observatorium-operator
      monitoring.open-cluster-management.io/name: "{{MULTICLUSTERMONITORING_CR_NAME}}"
  replicas: 1
  template:
    metadata:
      labels:
        control-plane: observatorium-operator
        monitoring.open-cluster-management.io/name: "{{MULTICLUSTERMONITORING_CR_NAME}}"
    spec:
      containers:
      - name: observatorium-operator
        image: quay.io/observatorium/observatorium-operator:master-2020-05-07-e07bb8e 
        imagePullPolicy: IfNotPresent
        args:
          - "--log-level=debug"
        resources:
          limits:
            cpu: 100m
            memory: 600Mi
          requests:
            cpu: 100m
            memory: 600Mi
      terminationGracePeriodSeconds: 10
