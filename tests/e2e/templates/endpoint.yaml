apiVersion: monitoring.open-cluster-management.io/v1alpha1
kind: EndpointMonitoring
metadata:
  name: endpoint-config
spec:
  global:
    serverUrl: "observatorium.hub"
  metricsCollectors:
  - enable: true
    type: OCP_PROMETHEUS
    relabelConfigs:
    - replacement: cluster1
      sourceLabels:
      - __name__
      targetLabel: cluster
    - replacement: test_value
      sourceLabels:
      - __name__
      targetLabel: test_key 